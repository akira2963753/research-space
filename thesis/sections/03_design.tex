\section{Proposed Method}
\label{sec:design}

\subsection{Dynamic Exponent-Window Accumulation}
We observed the distribution of partial sums in LLaMA2-7B (Fig.~\ref{fig:bfp4_exponent_dist}).
The distribution remains highly concentrated, indicating that a low-cost accumulator with a narrow dynamic range can be introduced prior to forwarding data to the FP-Acc.

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/bfp4_exponent_distribution.pdf}
    \caption{BFP4 partial-sum exponent distributions on three LLaMA2-7B layers (group size 16). The shaded band is the observed full span.}
    \label{fig:bfp4_exponent_dist}
\end{figure}

Specifically, we define an accumulation window
\begin{equation}
    [E_{\max} - T_{\mathrm{drop}},\; E_{\max} + T_{\mathrm{replace}}]
    \label{eq:window}
\end{equation}
where $E_{\max}$ denotes the exponent of the current maximum partial sum, and $T_{\mathrm{drop}}$ and $T_{\mathrm{replace}}$ are the drop and replace thresholds.
When a new partial sum arrives:
\begin{itemize}
    \item if its exponent falls below the lower bound, it is discarded;
    \item if it exceeds the upper bound, it directly replaces the value currently held in the accumulator;
    \item otherwise, it is accumulated using the low-cost accumulator.
\end{itemize}
This approach substantially reduces FP-Acc utilization and thereby lowers power consumption~\cite{bucketgetter2023,winacc2025}.
We refer to this method as \textbf{Dynamic Exponent-Window Accumulation (DEWA)}.

Although the majority of partial sums are concentrated, the overall distribution still exhibits a large dynamic range.
We attribute this to the influence of outliers, which cause the dynamic exponent window to shift significantly and thereby induce precision loss.
We therefore argue that outlier separation is essential as a complement to DEWA.
By separating activation outliers, a substantially higher proportion of partial sums fall within the accumulation window.

\subsection{Activation Top-2 Selection}
Outlier separation is therefore required, but the bi-exponent format of Section~\ref{subsec:bie} cannot be dropped into DEWA as is: its per-element type bits and four MAC cases add cost that the rare occurrence of outliers does not justify.
We instead retain the two exponents and remove the overhead through two changes:
\begin{enumerate}
    \item Since the majority of outliers concentrate on specific activation channels, applying bi-exponent encoding to weights yields benefits that do not justify the associated overhead.
    We therefore adopt single-exponent BFP for weights while retaining bi-exponent encoding for activations.
    \item We further examined the number of outliers occurring within each block (Table~\ref{tab:outlier_count}).
    99.4\% of G16 activation blocks contain at most two outliers.
    We propose \textbf{Activation Top-2 Selection}, wherein at most the two largest outliers are selected per block, further reducing hardware overhead.
\end{enumerate}

We call the resulting activation encoding \textbf{Top-2 activation-only bi-exponent BFP (T2A-BiE)}, shown in Fig.~\ref{fig:data_format}(c): the two indices identify which lanes of the G16 block use the outlier exponent, so no element carries a type bit.
Paired with 4-bit mantissas, the full configuration is denoted W-BFP4 / T2A-BiE4.

\begin{table}[t]
    \centering
    \caption{Outlier occupancy per G16 activation block (W-BFP4/A-BiE4, LLaMA2-7B).}
    \label{tab:outlier_count}
    \begin{tabular*}{\columnwidth}{@{\extracolsep{\fill}}ccc}
        \toprule
        Outliers per block & Block count & Block percentage \\
        \midrule
        0 & 21{,}437{,}737{,}318 & \textbf{88.60\%} \\
        1 & 2{,}373{,}974{,}147 & \textbf{9.81\%} \\
        2 & 241{,}189{,}169 & \textbf{1.00\%} \\
        $\geq 3$ & 141{,}941{,}990 & 0.59\% \\
        \bottomrule
    \end{tabular*}
\end{table}

Activation-only BiE and Top-2 capping have negligible impact on LLaMA2-7B perplexity relative to full BiE (Fig.~\ref{fig:ppl_delta_comparison}).

\begin{figure}[t]
    \centering
    \includegraphics[width=0.9\linewidth]{figures/ppl_delta_comparison.pdf}
    \caption{WikiText-2 $\Delta$PPL versus FP16 (G16). T2A-BiE stays close to full BiE and activation-only BiE.}
    \label{fig:ppl_delta_comparison}
\end{figure}

\subsection{Hybrid Top-2 DEWA}
\label{subsec:hybrid}
Top-2 keeps activation outliers from shifting $E_{\max}$, which is what DEWA needs to keep a narrow window.
We apply the two together and call the result \textbf{Hybrid Top-2 DEWA}: W-BFP4 / T2A-BiE4 encoding with the window of Eq.~\eqref{eq:window} on the normal accumulator.

A full 2-D sweep over every $(T_{\mathrm{drop}}, T_{\mathrm{replace}})$ pair is more than we need to pick a design point.
Fig.~\ref{fig:hybrid_delta_ppl} shows a representative slice: the symmetric corners $(8,8)$ and $(10,10)$, and a $T_{\mathrm{drop}}=9$ cut with $T_{\mathrm{replace}}$ from 9 down to 2.
Along this cut, $\Delta$PPL from the T2A-BiE4 baseline stays near 0.01 down to $(9,3)$ and then rises at $(9,2)$; $(8,8)$ is also worse.

Reducing $T_{\mathrm{replace}}$ shortens the integer window and cuts the dynamic range of the low-cost accumulator, which saves area.
$(9,3)$ is the smallest $T_{\mathrm{replace}}$ on this slice that remains on the accuracy plateau, so we take it as the design point that trades accumulator area against WikiText-2 loss.
Section~\ref{sec:architecture} implements the hybrid PE at $(T_{\mathrm{drop}}, T_{\mathrm{replace}})=(9,3)$.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.9\linewidth]{figures/hybrid_tskip_treplace_delta_ppl.pdf}
    \caption{WikiText-2 $\Delta$PPL of Hybrid Top-2 DEWA versus T2A-BiE4 (G16) on a representative $(T_{\mathrm{drop}}, T_{\mathrm{replace}})$ slice. $(9,3)$ is the selected design point.}
    \label{fig:hybrid_delta_ppl}
\end{figure}

