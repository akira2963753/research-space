# PPL-comparison figures

Active figures are regenerated by `../ppl_comparison.ipynb`. Each figure has a
600-DPI PNG and a same-stem vector PDF.

Group-16 figures in `g16/`:

- `ppl_delta_comparison.*`
- `hybrid_symmetric_ppl_log_zoom_t3-12.*`
- `hybrid_tskip_treplace_delta_ppl.*`

Four-model Vanilla-BFP comparisons in `g16_g32/`:

- `vanilla_bfp_delta_ppl_g16.*`
- `vanilla_bfp_delta_ppl_g32.*`
