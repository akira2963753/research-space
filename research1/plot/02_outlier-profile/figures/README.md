# Outlier-profile figures

This directory contains the active DEWA in-window-rate comparisons.

- `g16/`: Group-16 BFP4 profile.
- `g32/`: Group-32 BFP4 profile.
- Each active figure is stored as a 600-DPI PNG and a same-stem vector PDF.
- Outputs are regenerated by `../plot_outlier_profile.py`.
